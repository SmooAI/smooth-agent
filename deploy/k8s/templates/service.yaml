apiVersion: v1
kind: Service
metadata:
  name: {{ include "smooth-operator.fullname" . }}
  labels:
    {{- include "smooth-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: ws
      port: {{ .Values.service.port }}
      targetPort: ws
      protocol: TCP
  selector:
    {{- include "smooth-operator.selectorLabels" . | nindent 4 }}
